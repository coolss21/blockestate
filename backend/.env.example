# Backend
PORT=8081

# Frontend origin for dev (optional; backend also allows localhost + LAN :5173)
FRONTEND_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=demo_secret_change_me

# Hardhat
RPC_URL=http://127.0.0.1:8545
CHAIN_ID=31337
CONTRACT_ADDRESS=PUT_DEPLOYED_CONTRACT_ADDRESS_HERE

# Server signer (Hardhat account private key) used to sign certificate proofs
# (for demo you can paste one of the hardhat node private keys)
SIGNER_PRIVATE_KEY=0x...

# MongoDB (required for role dashboards)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=propertychain

# IPFS/Pinata (optional)
PINATA_JWT=
PINATA_GATEWAY=https://gateway.pinata.cloud/ipfs
